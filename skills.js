//function to convert Celsius
//to Fahrenheit use Celsius for C, Fahrenheit for F in method name

function CelsiusToFahrenheit(C) {
    return (C * 9/5) + 32;
}

// Driver code
let C = 42;
console.log(C + "°C is " + CelsiusToFahrenheit(C) + "°F");






